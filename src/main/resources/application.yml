spring:
  application:
    name: user-center
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/liang?useUnicode=true&characterEncoding=utf-8&useSSL=false
server:
  port: 8080